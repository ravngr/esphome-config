---

substitutions:
  # Device name and identifier (internal)
  device_name: float-display
  device_icon: mdi:toaster

  # Friendly name for UI
  friendly_name: Toast Car


packages:
  # Core
  core: !include packages/core_mqtt.yaml

  hardware_base: !include packages/hardware/generic_esp32_key/base.yaml
  hardware_status: !include packages/hardware/generic_esp32_key/status.yaml


mqtt:
  reboot_timeout: 5 min
  keepalive: 3s
